TODO Next:

At the page  http://localhost:3000/en/zenoh/book/core-concepts/key-expressions:
1. Remove Canocalization
2. In best practices, replace /{domain}/{entity}/{attribute} with {domain}/{entity}/{attribute}
3. replace robots/robot-42/pose with robot/42/pose